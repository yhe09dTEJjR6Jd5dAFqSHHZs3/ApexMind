交付：

一个压缩包

压缩包内只有main.py

main.py只有运行所必需的代码

已知：

网络连接正常

64位Windows11

已安装合适版本Python

基本要求：

因运行main.py新增的文件，尽量存放在用户选择的文件夹内

用户只能在D盘选择文件夹

用户点击“数值”按钮→用户选择屏幕上的一个数值→用户点击“确认”→开启AI

AI控制鼠标和键盘（不包括ESC）

目标：尽可能提高用户所选数值

用户按ESC→关闭AI

用户无需进行其他操作

高级要求：

为尽可能提升AI的决策能力，以及程序的适应能力，main.py应尽量减少硬编码

尤其对于AI控制相关内容，不应预先固定有效键盘按键、鼠标可交互区域等信息

AI应学习各个数值：

该数值的哪些属性会发生改变？
包括但不限于：格式、正负、绝对值、位置、颜色、尺寸、透明度、旋转角度

哪些因素会影响该数值？
包括但不限于：时间、屏幕画面、键盘操作、鼠标操作、其他数值

各影响因素与该数值各属性之间的关系

该数值各属性的变化规律
